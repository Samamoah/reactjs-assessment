import withRoot from '../../../modules/withRoot';
// --- Post bootstrap -----
import React from 'react';

function About() {
  return (
    <React.Fragment>
      <div>about</div>
    </React.Fragment>
  );
}

export default withRoot(About);
